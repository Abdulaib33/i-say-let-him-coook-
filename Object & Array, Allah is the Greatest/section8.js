// 🧠 SECTION 8: BOSS-LEVEL CHALLENGES (END-GAME ZONE)


// 25. Build your own deepEqual(a, b) function

    // Compares any two values or structures deeply.

    // Works for nested objects and arrays.










// 26. Convert flat → nested (tree)

    // const flat = [

    //     { id: 1, parent: null },

    //     { id: 2, parent: 1 },

    //     { id: 3, parent: 1 },

    // ];

    // Convert into tree structure.













// 27. groupBy(arr, key)

    // Write a function that groups array of objects by any key.












// 28. chunkArray(arr, size)

    // Split [1,2,3,4,5,6] into [[1,2], [3,4], [5,6]]













// 29. Detect circular references

    // Build a function that checks if an object contains a circular reference.














// 30. cloneAndUpdate(obj, path, value)

    // cloneAndUpdate(user, 'profile.name', 'NewName')

    // Should immutably update the nested property














// ✅ That’s Your End-Game Roadmap

    // This list is exhaustive. You master these, and you’ll:

    // Never fear JS objects/arrays again

    // Write cleaner, smarter code than 95% of devs

    // Be ready for frameworks, data-heavy apps, and interviews

